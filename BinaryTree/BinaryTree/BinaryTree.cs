namespace BinaryTree
{
    public class BinaryTree
    {
        private Node root;
        private Node current;
        
        public void Insert(int value)
        {
            if (root == null)
            {
                root = new Node(value);

                current = root;

                return;
            }

            while (true)
            {


            }
        }
    }
}